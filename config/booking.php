<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 管理者通知の宛先
    |--------------------------------------------------------------------------
    |
    | 詳細設計14章。Webhook処理・在庫戻し・メール送信の最終失敗、在庫差分検出時に
    | ここへ通知メールを送る。未設定の場合は通知を送らずログに警告を残す。
    |
    */

    'admin_notification_email' => env('BOOKING_ADMIN_NOTIFICATION_EMAIL'),

    /*
    |--------------------------------------------------------------------------
    | デモ運用（demo:reset）
    |--------------------------------------------------------------------------
    |
    | 詳細設計15.1。demo:reset が投入するシードの設定値。
    |
    | demo_slot_variant_ids は、生成する開催枠に割り当てる Shopify バリアントIDの
    | 一覧。slots.shopify_variant_id にはユニーク制約があるため使い回せず、
    | 生成する枠の数だけ実在の chanoka-demo ストアのバリアントIDが要る
    | （カンマ区切り）。未設定・不足分の枠は Shopify 未接続のまま draft で残る
    | （在庫確保できないため受付中にできない）。
    |
    | demo_password は要件7.4の「デモアカウントを公開ページに記載する」運用のため、
    | 秘匿しない前提の値（コミット済みの既定値でよい）。
    |
    */

    'demo_slot_variant_ids' => array_filter(explode(',', (string) env('BOOKING_DEMO_SLOT_VARIANT_IDS', ''))),

    'demo_password' => env('BOOKING_DEMO_PASSWORD', 'demo-pass-1234'),

];
